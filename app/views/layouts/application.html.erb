<!DOCTYPE html>
<html>
  <head>
    <title>GreenWheeler</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

  </head>

  <body>
    <%= render "shared/navbar" %>
    <%= yield %>
    <%= render 'shared/flashes' %>
  </body>
</html>
